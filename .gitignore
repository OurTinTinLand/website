# 依赖
node_modules/

# 构建产物
dist/

# 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# 环境 / 临时
.env
.env.*
!.env.example
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# IDE / Claude 客户端配置
.claude/

# 测试 / 覆盖率
coverage/
*.lcov

# TypeScript
*.tsbuildinfo
# GitHub Pages staging directory (produced by .github/workflows/deploy.yml)
.github/_site/

# PocketBase data directory (regenerated by pb on startup)
backend/pb_data/
backend/pb_data.*/
backend/pb_public/

# Adobe Illustrator source files (binary, regenerated from source)
*.ai

# Generated PDFs at repo root (not part of build)
/*.pdf

# Root-level brand exploration PNGs (kept in spec/assets/ instead)
/Tintin*.png
/tintin_dog.png
/tintin_white_circle.png

# Old shirt design source (32MB binary)
/tintin_shirt_0715.ai

# Separate Codex pet pipeline (frames / qa / references — not part of main site)
/tintin-pet-run/
